# 业务实体知识

cgroup_sched_plugin 模块核心业务实体的完整知识定义，涵盖静态概念、动态运行、规则约束与代码映射。

## 调度策略组（SchedPolicy）

### 实体概念

| 子维度   | 内容                                                                                     |
| ----- | -------------------------------------------------------------------------------------- |
| 实体名称  | 调度策略组 / SchedPolicy                                                                    |
| 实体定义  | 对进程/线程归属的 cgroup 子组的逻辑抽象，决定进程在 CPU 调度上的优先级档位                                          |
| 核心特征  | 取值固定 5 类：SP_DEFAULT=0、SP_BACKGROUND=1、SP_FOREGROUND=2、SP_SYSTEM_BACKGROUND=3、SP_TOP_APP=4；上限 SP_UPPER_LIMIT=0xff |

### 上下文与场景

| 子维度  | 内容                                                                      |
| ---- | ----------------------------------------------------------------------- |
| 交互流程 | 事件处理层修改进程的 `SchedPolicy` → 执行层 `ComputeProcessGroup` 计算最终组别 → `ApplyProcessGroup` 下发到内核 cgroup |

### 知识关联

| 子维度   | 内容                                                                  |
| ----- | ------------------------------------------------------------------- |
| 下游影响  | `ProcessRecord::curSchedGroup_`（进程当前组别） |
| 平级关联  | `AdjustSource`（触发重算的语义来源）                                          |

### 代码与符号

| 子维度 | 内容                                                                                  |
| --- | ----------------------------------------------------------------------------------- |
| 核心类 | `SchedPolicy`（`framework/process_group/include/sched_policy.h`）                     |

---

## 进程记录（ProcessRecord）

### 实体概念

| 子维度   | 内容                                                                                                       |
| ----- | -------------------------------------------------------------------------------------------------------- |
| 实体名称  | 进程记录 / ProcessRecord                                                                                    |
| 实体定义  | 描述一个进程的运行态快照，是策略计算的唯一数据源                                                                               |
| 核心特征  | 持有 uid、pid、进程名、进程类型、进程状态、能力列表、窗口列表、焦点、可见性、瞬态任务、连续任务、音频播放/采集、相机、蓝牙、Wi-Fi、MMI、运行锁、屏幕录制、视频、Nap、键线程、序列号、宿主 pid 等字段 |
| 类型/分类 | `ProcRecordType`：NORMAL=0、EXTENSION=1、RENDER=2、GPU=3、CHILD=4                                            |

### 上下文与场景

| 子维度  | 内容                                                                                            |
| ---- | --------------------------------------------------------------------------------------------- |
| 交互流程 | 进程创建事件触发创建 → 事件处理层各 `Handle*` 更新字段 → 进程死亡事件移除 → 执行层 `ComputeProcessGroup` 读取字段计算 `SchedPolicy` |
| 状态流转 | `lastSchedGroup_`/`curSchedGroup_`/`setSchedGroup_`/`specialSchedGroup_` 记录调度组别变迁             |

### 规格与约束

| 子维度 | 内容                                                                                            |
| --- | --------------------------------------------------------------------------------------------- |
| 业务规则 | 应用名与进程名仅在为空时设置，已设置后不覆盖；附属进程（渲染/GPU/子进程）的组别跟随宿主进程；`curSchedGroup_` 仅在下发成功后更新 |
| 并发约束 | 状态变更必须在 `CgroupEventHandlerQueue` 队列任务内执行                                                    |

### 知识关联

| 子维度   | 内容                                                |
| ----- | ------------------------------------------------- |
| 上层依赖  | `Application`（uid 维度的进程集合）                        |
| 下游影响  | `SchedPolicy`（策略计算输入）       |
| 平级关联  | `AbilityInfo`、`WindowInfo`（能力与窗口状态影响策略计算）         |

### 代码与符号

| 子维度 | 内容                                                                          |
| --- | --------------------------------------------------------------------------- |
| 核心类 | `ProcessRecord`、`Application`、`AbilityInfo`、`WindowInfo`（`framework/sched_controller/include/supervisor.h`） |

---

## 调度策略计算（ComputeProcessGroup）

### 实体概念

| 子维度   | 内容                                                                                                       |
| ----- | -------------------------------------------------------------------------------------------------------- |
| 实体名称  | 调度策略计算 / ComputeProcessGroup                                                                            |
| 实体定义  | 执行层基于 `ProcessRecord` 字段按优先级规则计算进程应归属 `SchedPolicy` 的决策过程                                                  |
| 核心特征  | 纯函数语义；按优先级顺序判断：渲染进程取宿主组 → 进程创建取默认 → 焦点进程取顶级应用 → 无能力按进程状态 → 可见取前台 → Service Extension 按进程状态 → 其他按进程状态 |

### 上下文与场景

| 子维度  | 内容                                                              |
| ---- | --------------------------------------------------------------- |
| 交互流程 | 事件处理层触发 `AdjustProcessGroup` → `ComputeProcessGroup` 计算 → `ApplyProcessGroup` 下发 |

### 规格与约束

| 子维度 | 内容                                                                                              |
| --- | ----------------------------------------------------------------------------------------------- |
| 业务规则 | 渲染进程：取宿主进程当前组，宿主不存在取默认；焦点进程：当前应用焦点进程取顶级应用组；无能力记录：进程状态后台取后台组，否则默认；可见窗口：取前台组；Service Extension：后台取后台组，否则默认；其他：后台取后台、前台取前台、其他取默认 |
| 幂等约束 | 仅当新组别与当前组别不同时才执行实际下发                                                                            |

### 知识关联

| 子维度   | 内容                                                          |
| ----- | ----------------------------------------------------------- |
| 上层依赖  | `ProcessRecord`（策略输入）、`AdjustSource`（触发来源）                 |
| 下游影响  | `ApplyProcessGroup`（下发执行）、`ReportArbitrationResult`（仲裁上报） |

### 代码与符号

| 子维度 | 内容                                                                          |
| --- | --------------------------------------------------------------------------- |
| 核心方法 | `CgroupAdjuster::ComputeProcessGroup`、`AdjustProcessGroup`、`ApplyProcessGroup`（`framework/sched_controller/cgroup_adjuster.cpp`） |

---

## 调整来源（AdjustSource）

### 实体概念

| 子维度   | 内容                                                                                                       |
| ----- | -------------------------------------------------------------------------------------------------------- |
| 实体名称  | 调整来源 / AdjustSource                                                                                      |
| 实体定义  | 触发策略重算的语义来源枚举，参与仲裁上报与 HiSysEvent 追踪                                                                      |
| 核心特征  | 取值覆盖前台应用变更、能力状态、Extension 状态、进程创建、瞬态任务开始/结束、连续任务开始/结束、窗口获焦/失焦、窗口可见性变更、关键显示线程上报、自渲染线程、窗口状态变更、音频状态变更、Webview 音频状态变更、进入/退出 Nap、进程状态、屏幕录制、Webview 屏幕录制、Webview 进入 Nap、系统负载等级变更、应用预加载、重新计算、应用保活、ServiceExtension 连接/更新状态、屏幕状态、能效资源状态变更、UIExtension 预加载；上限 ADJS_UPLIMIT=255 哨兵 |

### 规格与约束

| 子维度 | 内容                                                |
| --- | ------------------------------------------------- |
| 业务规则 | 新增触发源须插入到 `ADJS_END` 之前，保留 `ADJS_UPLIMIT=255` 哨兵 |

### 代码与符号

| 子维度 | 内容                                                                          |
| --- | --------------------------------------------------------------------------- |
| 核心枚举 | `AdjustSource`（`framework/sched_controller/include/cgroup_adjuster.h`）      |
